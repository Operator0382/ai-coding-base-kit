# OpenClaw Adapter

OpenClaw should load `AGENTS.md`, then the selected role file under
`agent-system/roles/`. Follow the shared lifecycle, ownership policy, and handoff
contract. This profile intentionally assumes no undocumented OpenClaw-specific
configuration format.
Also load the shared security policy. No runtime-specific delegation feature is
required.
