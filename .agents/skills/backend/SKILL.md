---
name: backend
description: Implement the backend track of an architected feature. Use when the assigned feature requires APIs, server validation, persistence, authorization, or a backend data contract.
---

# Backend Track

Use this skill only as a Codex entry point to the canonical backend workflow. Load and
follow `AGENTS.md`, `agent-system/README.md`,
`agent-system/roles/backend.md`, `agent-system/workflows/lifecycle.yaml`,
`agent-system/policies/ownership.md`, `agent-system/policies/security.md`,
`agent-system/contracts/handoff.md`, `features/INDEX.md`, the assigned feature
specification, and relevant server code.

Confirm the backend track is required and the current lifecycle state permits work.
Stay within the canonical file scope, publish the required data contract, and record
the completed track handoff in the feature specification.
